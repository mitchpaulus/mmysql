This is a new CLI tool for interacting with MySql databases.
It is cross platform and written in golang.

It should focus on absolute simplicity for my common tasks.

For example, by default, the response should be in JSON for easy post processing.

Prefer as few dependencies as possible. If it makes sense to have a dependency, use the golang sponsored libraries over true third-party ones.
